# Base URL of the API. In dev this is proxied by Vite, so it can stay empty.
# In production, point this at your deployed API, e.g. https://api.yourdomain.com
VITE_API_BASE_URL=https://picnic-api.onrender.com
